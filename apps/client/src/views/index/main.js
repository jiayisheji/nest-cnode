import './style.scss';

console.log('hello index main');

/* $(() => {
  console.log(logo.default);
  // $('body').append($('img').attr('src', logo.default));
  // $('body').append($('img').attr('src', ddd.default));
  // $('body').append($('img').attr('src', ddd.default));
});
 */
